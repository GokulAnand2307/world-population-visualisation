# Data setup

Export the required indicators from the World Bank DataBank and save the CSV as `world_population_data.csv` in this directory.

The analysis expects `Country Name`, `Series Name` and yearly columns. The original source file is excluded from version control so users retrieve the latest data and retain the World Bank source context.

